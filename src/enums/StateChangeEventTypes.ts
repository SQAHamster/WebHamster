export enum StateChangeEventType {
    resume = "resume",
    pause = "pause",
    abort = "abort",
    undo = "undo",
    redo = "redo",
    speedChange = "speedChange",
    inputResponse = "inputResponse",
    all = "all"
}